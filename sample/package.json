{
  "name": "sample",
  "description": "sample package for testing hardhat-multibaas-plugin",
  "version": "1.0.0",
  "author": "Curvegrid",
  "license": "MIT",
  "scripts": {
    "test": "npx hardhat test",
    "deploy:metacoin": "HARDHAT_NETWORK=development npx hardhat deploy --contract metacoin",
    "deploy:greeter": "HARDHAT_NETWORK=development npx hardhat deploy --contract greeter"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts-upgradeable": "^4.9.5",
    "hardhat": "^2.19.5",
    "hardhat-multibaas-plugin": "./.."
  }
}
